- Curry up this problem
  * checkCombo(decrement(), increment(), x, y)
  * From this we need to know the GAMESTATE as well as a location for block if needed
  * We could manipulate a closer and return or return an object



- Need to understand what the default of an enum will be
